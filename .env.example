# ============================================
# Настройки приложения
# ============================================
PROJECT_NAME=FastAPI Template
VERSION=1.0.0
API_V1_STR=/api/v1

# ============================================
# Настройки базы данных PostgreSQL
# ============================================
# Хост базы данных
# Для локальной разработки: localhost или 127.0.0.1
# Для Docker: postgres (имя сервиса в docker-compose.yml)
POSTGRES_SERVER=localhost
# Порт базы данных
# Для локальной разработки: 5433 (если используете Docker для БД)
# Для Docker: 5432 (внутренний порт контейнера)
POSTGRES_PORT=5432
# Имя пользователя базы данных
POSTGRES_USER=postgres
# Пароль базы данных
POSTGRES_PASSWORD=postgres
# Имя базы данных
POSTGRES_DB=fastapi_db

# ============================================
# Настройки CORS (разрешенные источники)
# ============================================
# Список разрешенных источников
# Формат: JSON-массив ["http://localhost:5173","http://localhost:3000"]
# Или строка через запятую: http://localhost:5173,http://localhost:3000
BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000"]

# ============================================
# Настройки Frontend (Vite)
# ============================================
# URL бэкенда для фронтенда
# Для локальной разработки: http://localhost:8000
# Для Docker: http://backend:8000 (имя сервиса в docker-compose.yml)
VITE_API_BACKEND_URL=http://localhost:8000
